version: "3.8"
services:
  rag-eval-studio:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - rag_data:/app/data
      - rag_qdrant:/app/qdrant_data
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always

volumes:
  rag_data:
  rag_qdrant:
